# Server
NODE_ENV=development
PORT=3000

# Database (PostgreSQL)
# Local: localhost | Docker Compose API service: use host `db`
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/job_match?schema=public"
