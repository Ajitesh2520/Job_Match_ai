// Placeholder schema — Candidate / Job models will be added in a later iteration.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
